1. Auto-Population on Boarding Intake Record Creation (Adobe Integration)

Given a Boarding Intake record is created via Adobe form submission
When the record is generated in Salesforce
Then all mapped fields from the Adobe form should be auto-populated on the Boarding Intake record
And data should reflect exactly as received from Adobe without manual intervention

7. Field Mapping Accuracy

Given field mappings are defined between Boarding Intake and downstream objects
When records are created
Then each field should map accurately to its corresponding field
And no data truncation or transformation errors should occur
